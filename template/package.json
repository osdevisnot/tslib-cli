{
  "name": "@template/monorepo",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "postbootstrap": "yarn run build",
    "build": "lerna exec -- tslib build",
    "coverage": "tslib coverage",
    "install": "yarn run bootstrap",
    "setup": "tslib setup",
    "sort": "sort-package-json",
    "postsort": "lerna exec -- sort-package-json",
    "test": "tslib test"
  },
  "prettier": "@osdevisnot/prettier",
  "devDependencies": {
    "@osdevisnot/prettier": "^0.1.0",
    "@types/jest": "latest",
    "@types/node": "latest",
    "lerna": "^3.15.0"
  }
}
